<block wx:if="{{$taroCompReady}}">
    <view class="postcard">
        <view class="postcard-nav">
            <at-nav-bar compid="{{$compid__29}}"></at-nav-bar>
        </view>
        <view class="postcard-tab">
            <view class="tab-cnt" style="{{anonymousState__temp}}">
                <view class="cnt-ic" style="{{anonymousState__temp2}}">
                    <at-icon compid="{{$compid__30}}"></at-icon>
                </view>
                <view class="cnt-text" style="{{anonymousState__temp3}}">{{dataList.likes}}</view>
            </view>
            <view class="tab-cnt" style="{{anonymousState__temp4}}">
                <view class="cnt-ic" style="{{anonymousState__temp5}}">
                    <at-icon compid="{{$compid__31}}"></at-icon>
                </view>
                <view class="cnt-text" style="{{anonymousState__temp6}}">{{dataList.visits}}</view>
            </view>
            <view class="tab-cnt" style="{{anonymousState__temp7}}">
                <view class="cnt-ic" style="{{anonymousState__temp8}}">
                    <at-icon compid="{{$compid__32}}"></at-icon>
                </view>
                <view class="cnt-text" style="{{anonymousState__temp9}}">{{anonymousState__temp10}}</view>
            </view>
        </view>
        <view class="postcard-centent">
            <htmltowxml linenums="{{false}}" showLoading="{{false}}" text="{{dataList.originalContent}}" highlightStyle="dracula" type="md"></htmltowxml>
        </view>
    </view>
</block>